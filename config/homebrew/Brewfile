# ~/.config/homebrew/Brewfile
# Declarative source of truth for Homebrew's layer (system + GUI apps).
# Apply / reconcile with:  brew bundle --file=~/.config/homebrew/Brewfile
# Dev tooling + AI CLIs are deliberately NOT here — those live in mise
# (~/.config/mise/config.toml). One package manager per layer, no overlap.

# --- CLI: Omarchy's pacman set, ported ---
brew "bat"          # cat with syntax highlighting; used as MANPAGER
brew "eza"          # ls replacement, aliased over ls
brew "fd"
brew "fzf"
brew "ripgrep"
brew "zoxide"       # aliased over cd
brew "jq"
brew "btop"
brew "fastfetch"
brew "starship"
brew "tmux"
brew "neovim"
brew "lazygit"
brew "lazydocker"
brew "dua-cli"
brew "tlrc"         # tldr
brew "yt-dlp"
brew "imagemagick"
brew "ffmpeg"
brew "gifski"       # high-quality GIF encoder (Omarchy 3.8 gif transcoding)
brew "wget"
brew "mise"         # dev-tooling layer manager
brew "pam-reattach"  # Touch ID for sudo inside tmux
brew "mas"          # Mac App Store CLI — used by `macup`
brew "coreutils"    # GNU coreutils; some scripts assume GNU flags
brew "gnu-sed"
brew "findutils"

# --- CLI already on this machine (kept) ---
brew "fx"
brew "hf"
brew "just"
brew "llama.cpp"
brew "mole"
brew "openai-whisper"
brew "whisper-cpp"
brew "xh"

# --- GUI casks ---
cask "ghostty"                 # terminal
# google-chrome deliberately NOT a cask on this Mac: it's installed root-owned
# and self-updates via system-wide Keystone (/Library/Google), so brew can
# neither upgrade nor reinstall it — greedy upgrades just error on it.
cask "iina"                    # media player (macOS-native mpv)
cask "localsend"
cask "tailscale-app"           # mesh VPN (menu-bar app; ships its own CLI)
cask "transmission"            # replaced qbittorrent (Gatekeeper-deprecated, disabled 2026-09-01)
cask "aerospace"               # i3-style tiling WM (no SIP disable needed)
cask "karabiner-elements"      # Caps Lock -> Option remap (config/karabiner)
cask "raycast"                 # launcher
cask "cursor"
cask "visual-studio-code"
cask "zed"
cask "cmux"
cask "t3-code@nightly"         # T3 Code nightly — multi-agent control plane (t3.codes)
cask "betterdisplay"
cask "cleanshot"
cask "ollama-app"
cask "lm-studio"               # local LLMs: MLX (fastest on Apple Silicon) + GGUF
cask "obsidian"
cask "obs"
cask "losslesscut"             # video/screen-recording trimmer (Omacut equivalent)
cask "chatgpt"                 # ChatGPT desktop app (Omarchy 4 Install > AI)
cask "claude"                  # Claude desktop app
cask "1password"
cask "1password-cli"
cask "libreoffice"
cask "kdenlive"
cask "moonlight"
cask "steam"
cask "docker-desktop"          # installed (needs admin password on install)
cask "font-jetbrains-mono-nerd-font"
cask "font-ia-writer-duo"

# NOTE: claude-code, codex (casks) and gh (formula) intentionally omitted —
# migrated to mise so a single update verb bumps them. See mise config.toml.
